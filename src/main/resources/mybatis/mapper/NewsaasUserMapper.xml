<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pkuokuo.jwtdemo.mapper.NewsaasUserMapper">
    <select id="selectUserByPhoneAndPwd" resultType="com.pkuokuo.jwtdemo.entity.NewsaasUser">
        SELECT * FROM `newsaas_user` WHERE phone = #{phone}
    </select>

    <select id="selectUserByPhone" resultType="com.pkuokuo.jwtdemo.entity.NewsaasUser">
        SELECT * FROM `newsaas_user` WHERE phone = #{phone}
    </select>



</mapper>